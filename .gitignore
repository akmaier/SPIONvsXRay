# Copyrighted source article (do not redistribute)
*.pdf

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.ipynb_checkpoints/

# CONRAD / pyconrad runtime artifacts
Conrad.xml
*.jar
.pyconrad/

# Simulation outputs (regenerable)
results/
data/*.tif
data/*.tiff
data/*.raw
data/*.npy

# OS / editor
.DS_Store
.idea/
.vscode/

# Local arm64 JDK 8 (Zulu) for pyconrad/JVM — not committed
.jdk8/

# Local OpenCL shim (Path A GPU enablement)
.jogamp/

# RabbitCT benchmark data (large, fetched from LME; not redistributed)
data/rabbitct/
data/RabbitCT/
*.rctd

# Compiled CONRAD extension (build artifacts; source is tracked)
conrad_ext/out/

# Secrets (Zenodo token etc.) — never commit
publish/rabbitct/zenodo_secrets.env
*.secret

# CONRAD re-release artifacts (large binaries; host on FAU, not git)
publish/conrad/*.jar
publish/conrad/*.zip

# LaTeX build artifacts
paper/*.aux
paper/*.log
paper/*.out
paper/*.fls
paper/*.fdb_latexmk
paper/*.synctex.gz

# ...but DO track the paper's own figures and compiled draft (not the copyrighted article)
!paper/figures/*.pdf
!paper/spie_manuscript.pdf

# Reviewer comment files (Stefan/Lukas V2 review) -- not tracked
Allgemeine Anmerkungen zur V2_SL.docx
spie_manuscript_V2_SL.pdf
